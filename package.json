{
  "name": "@index",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "sherif && turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "sherif": "sherif",
    "sherif:fix": "sherif --fix",
    "check": "turbo run lint && turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.6.0",
    "sherif": "^1.6.1",
    "turbo": "^2.5.4",
    "typescript": "5.8.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
